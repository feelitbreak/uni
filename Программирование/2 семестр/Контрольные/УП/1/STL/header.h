#include <list>
using namespace std;
void Menu();
bool Input(list<double>&, const char[]);
void Output(list<double>&);
void AddElements(list<double>&);
void FindGreater(list<double>&);
void Multiply(list<double>&);